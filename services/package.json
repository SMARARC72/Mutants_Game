{
  "name": "mutants-game-services",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Vercel serverless layer (TDD §7): OpenAI gen proxy (idempotent generate-once, ADR-007), Succession publish/fetch. Holds privileged keys (OpenAI, Supabase service-role) as Vercel env secrets — NEVER shipped to the client.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "typescript": "^5.6.2",
    "vitest": "^2.1.2"
  }
}
