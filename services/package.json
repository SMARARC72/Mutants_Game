{
  "name": "mutants-game-services",
  "version": "0.0.0",
  "private": true,
  "description": "Vercel serverless layer (TDD §7): OpenAI gen proxy, Succession publish/fetch, deterministic re-validation, companion web. Scaffold only in Phase 0; functions land in Phase 4.",
  "scripts": {
    "lint": "echo \"(services lint placeholder — wired in Phase 4)\"",
    "build": "echo \"(services build placeholder — wired in Phase 4)\""
  },
  "engines": {
    "node": ">=18"
  }
}
